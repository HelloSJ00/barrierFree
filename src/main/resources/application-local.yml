spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test_db  # MongoDB 연결 URI
      auto-index-creation: true  # 인덱스를 자동으로 생성
  datasource:
    url: jdbc:mysql://localhost:3306/test_db
    username: root
    password: dh7672@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging.level:
  org.hibernate.type: trace

# application.yml
ai:
  url: "https://your-ai-service-url.com"

